var config ={
	host : 'localhost',
	user : 'root',
	password : '',
	database : 'washapp',
	//Le pasamos el siguiente parametro para mostrar la fecha en otro formato
	dateStrings : true
};

module.exports = config;